CREATE TABLE book (
                      id VARCHAR(255) NOT NULL,
                      title VARCHAR(255),
                      author VARCHAR(255),
                      private_price DOUBLE PRECISION NOT NULL,
                      PRIMARY KEY (id)
);